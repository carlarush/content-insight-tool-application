package com.carla.contentinsighttool.repository;

import com.carla.contentinsighttool.entity.CheckinBatch;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CheckinBatchRepository extends JpaRepository<CheckinBatch, Long> {}